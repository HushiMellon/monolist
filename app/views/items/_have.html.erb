
<%= link_to "" , ownerships_path(item_id: item.id ,id: current_user, type: "Have") ,
                      :class => "have-#{item.id} btn-have btn btn-warning btn-sm have",
                      :remote => true,
                      :method => :post %>
